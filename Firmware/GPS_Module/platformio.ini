[env:stm32_canbus]
platform = ststm32
board = genericSTM32F103CB
board_build.mcu = stm32f103cbt6
framework = arduino
debug_tool = stlink
upload_protocol = stlink

upload_flags =
    -c
    adapter serial 37FF71064E57343636F11443

build_flags =
    -DHAL_CAN_MODULE_ENABLED
    ; Set HSE based on physical crystal
    -DHSE_VALUE=16000000U
    -Wall
    -Wextra
    -Werror
    ; TinyGPS++ emits an intentional fallthrough warning under this toolchain.
    -Wno-implicit-fallthrough
    -fno-exceptions

lib_deps =
    mikalhart/TinyGPSPlus @ ^1.1.0
    https://github.com/PaulStoffregen/SerialFlash.git

lib_extra_dirs =
    ; Explicitly select only required libraries from the local monorepo
    ../../../av-libraries/
